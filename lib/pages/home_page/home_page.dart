// ignore_for_file: prefer_const_constructors, avoid_unnecessary_containers, prefer_const_literals_to_create_immutables

import 'package:flutter/material.dart';
// import 'package:iconsax_flutter/iconsax_flutter.dart';

class HomePage extends StatefulWidget {
  const HomePage({super.key});

  @override
  State<HomePage> createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  // listes des données dynamiques et les variables à mettre ici

  @override
  Widget build(BuildContext context) {
    return Scaffold(

        // screen (avatar + search_bar + notification_icon)

        // screen (sold + option for (send, deposit , market, more))

        // screen for recent activities

        // screen for bottom navigation bar
        );
  }
}
